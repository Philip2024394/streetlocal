/**
 * Firebase stub — Firebase has been removed.
 * All features are being migrated to Supabase.
 * These null exports keep existing code from crashing during migration.
 */
export const auth = null
export const db = null
export const functions = null
