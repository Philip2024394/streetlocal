/**
 * Compat shim — the canonical source of truth is now
 * shared/themes/foodProThemes.js so the landing site can render the
 * Pro carousel + themes page from the same data.
 *
 * All existing foodlocal-pro imports keep working unchanged.
 */
export { FOOD_CATEGORIES_FULL } from '@shared/themes/foodProThemes'
